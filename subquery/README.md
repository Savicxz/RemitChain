# RemitChain SubQuery (Scaffold)

## Files
- `project.yaml` configuration
- `schema.graphql` entities
- `src/mappings/remittance.ts` mapping stubs

## Next Steps
- Replace `chainId` and `endpoint` in `project.yaml`.
- Implement entity writes in mapping handlers.
- Run `subql codegen` and `subql build` after installing SubQuery CLI.
